<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main_nav_graph"
    app:startDestination="@id/profileFragment">

    <fragment
        android:id="@+id/profileFragment"
        android:name="com.blogspot.soyamr.recipes3.presentation.profile.ProfileFragment"
        android:label="fragment_main_rent"
        tools:layout="@layout/fragment_profile">
        <action
            android:id="@+id/action_profileFragment_to_authorization_nav_graph"
            app:destination="@id/authorization_nav_graph" />
    </fragment>
    <include app:graph="@navigation/authorization_nav_graph" />
</navigation>